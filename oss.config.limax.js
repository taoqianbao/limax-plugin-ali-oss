/**
 * 阿里云 OSS 配置文件
 * 请替换以下配置为你的实际配置
 */
module.exports = {
  // 阿里云 AccessKey ID
  accessKeyId: 'your-access-key-id',

  // 阿里云 AccessKey Secret
  accessKeySecret: 'your-access-key-secret',

  // OSS Bucket 名称
  bucket: 'your-bucket-name',

  // OSS Region 地域（默认：oss-cn-hangzhou）
  region: 'oss-cn-hangzhou',

  // OSS 上传目录（默认：images）
  uploadPath: 'images'
};